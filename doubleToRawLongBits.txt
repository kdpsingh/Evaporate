var data = [2.3, 32.3];
var hash = 0;
if (data != null)
	for (var index5603 = 0; index5603 < data.length; index5603++) {
		var d = data[index5603];
                hash ^= (function (f) { var buf = new ArrayBuffer(4); (new Float32Array(buf))[0] = f; return (new Uint32Array(buf))[0]; })(Math.fround(d)) >> 6;
	}
